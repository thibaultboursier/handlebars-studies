define([], function () {
    return (function () {
        /**
         * return {Object}
         */
        var loading = (function () {
            var _loader;

            /** Show loader */
            function show() {
            }

            /** Hide loader */
            function hide() {
            }

            return {
                show: show,
                hide: hide
            }
        })();

        /** Init */
        function init() {
        }

        return {
            init: init
        }
    })();
});
